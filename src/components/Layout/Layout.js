import React from 'react';
import PropTypes from 'prop-types';
import './Layout.css';

const Layout = () => (
  <div className="Layout">
    Layout Component
  </div>
);

Layout.propTypes = {};

Layout.defaultProps = {};

export default Layout;
